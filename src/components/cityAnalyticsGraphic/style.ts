import styled from "styled-components";

const StyledCityAnalyticsGraphic = styled.div`
  background-color: #fff;
  width: 85%;
  margin: 0 auto;
  border-radius: 4px;

  .cityAnalytics-wrapper {
    padding: 0 .4rem;
  }

  @media (max-width: 1200px) and (min-width: 570px) {
    width: 95%;
  }
`;

export default StyledCityAnalyticsGraphic;
